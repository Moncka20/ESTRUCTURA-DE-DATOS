/*
let nombredelobjeto ={
    llave:"valor",
}

let obj ={
    nombre:"carlos",
    edad:12,
    notas:[4,3,2]
}
let obj2 ={
    nombre:"maria",
    edad:14,
    notas:[4,2,1]
}

let objetos =[]
objetos.push(obj,obj2)

let suma = 0
for (let i = 0; i < objetos.length; i++) {
    suma+=objetos[i].edad
}
console.log("el promedio de edades es"+ (suma/objetos.length));

*/
